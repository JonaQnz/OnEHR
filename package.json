{
  "name": "formbuilder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:packages": "npm run build --workspace=core && npm run build --workspace=plugin-api && npm run build --workspace=formbuilder-example-vitals-plugin --workspace=formbuilder-example-n8n-plugin --workspace=formbuilder-plugin-aql-prefill --workspace=formbuilder-plugin-iframe --if-present",
    "test": "npm run build:packages && npm run test --workspace=api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
