{
  "name": "formbuilder-example-n8n-plugin",
  "version": "1.0.0",
  "description": "Example Form Builder plugin that provisions an n8n webhook-to-EHRbase workflow from form settings",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build --workspace=core && npm run build --workspace=openehr-engine && npm run build --workspace=plugin-api && npm run build && node --test tests/*.test.js"
  },
  "license": "ISC",
  "dependencies": {
    "plugin-api": "*",
    "core": "*",
    "openehr-engine": "*",
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
