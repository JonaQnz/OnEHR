{
  "name": "openehr-engine",
  "version": "1.0.0",
  "description": "Pure openEHR mapping and form-definition helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build --workspace=core && npm run build && node --test tests/*.test.js"
  },
  "dependencies": { "core": "*" },
  "devDependencies": { "typescript": "^5.7.3" }
}
